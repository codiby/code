# @codiby/mobile

React Native client (planned). Connects directly to `@codiby/core` daemons over
HTTPS + bearer token — one of the "master" clients in the star topology
alongside `@codiby/desktop`.

Empty placeholder for now.
